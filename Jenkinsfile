pipeline {
  agent any
  stages {
    stage('initialize') {
      steps {
        echo 'initialize'
      }
    }

    stage('build') {
      steps {
        echo 'build'
      }
    }

    stage('unit test') {
      steps {
        echo 'unit test'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }

  }
}